Scale OpenGL viewer distance fog to the camera far plane instead of a hardcoded 20 m to 200 m range. Shapes past 200 m were mixed to exactly the background colour and became invisible while still well inside the 1000 m far plane, which read as geometry failing to render.
