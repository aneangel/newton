Disable the OpenGL viewer spotlight by default so scene lighting no longer depends on camera position. The spotlight cone was anchored to the camera, which stripped all direct light from geometry outside it and made distant robots and ground appear unlit. Re-enable it with `viewer.renderer.spotlight_enabled = True`.
