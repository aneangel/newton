Stop the OpenGL viewer sky shell from writing depth. The sky is a sphere of radius `0.9 * far` centred on the camera, so writing depth occluded every shape and every part of the ground beyond that radius and cut the scene along a camera-centred sphere, which appeared as a curved horizon that moved with the camera.
